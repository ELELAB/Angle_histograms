#example reported in the publication
#to run the example you need to untar the chain1.tar.gz and chain2.tar.gz folders
#run with:
python chi_histo_plot.py -f config.cfg
#it will generate different pdf reports with dihedral scans from each dihedral class (i.e. phi, psi etc)
